import { Navbar } from "../UI/Navbar";
import { SearchBar } from "../UI/SearchBar";

function Header({}){
    return(
        <header>
            <Navbar/>
            <SearchBar/>
        </header>
    );
}